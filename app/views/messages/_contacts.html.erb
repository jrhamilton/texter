<div id="ajax_contacts">
  <% Contact.all.each do |contact| %>
    <li><%= contact.name %>: (<%= contact.number[0..2] %>)-<%= contact.number[3..5] %>-<%= contact.number[6..9] %></li>
  <% end %>
</div>
